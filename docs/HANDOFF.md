# 給 Haley 的交接文件

這份文件是寫給**不是工程師**的新娘看的，目的是讓你接手這個婚禮邀請網站之後，自己就能完成「開通」跟「換成真實資料」，不需要懂寫程式。照著順序做就好，每一步都有寫清楚要點哪裡。

---

## 1. 這是什麼

這是一個婚禮邀請函網站（線上喜帖），網址開起來上半是邀請函、下半是「出席意願表單」，賓客填完按送出，資料會自動寫進一份 Google 試算表。網站本身不用另外裝主機、不用另外寫後端，全部是免費的：網站放在 GitHub Pages，表單資料的接收放在你自己的 Google Apps Script（Google 帳號內建、免費）。

---

## 2. 接收 repo 後第一次要做的事

這個 repo（程式碼倉庫）會用 GitHub 的「Transfer repository」功能，從 Chian 的帳號 `chi-an-yang` 轉移到你的 GitHub 帳號。轉移信寄到你信箱之後，照下面順序做：

1. **接受轉移信**：信箱裡會收到一封 GitHub 的 repository transfer 邀請信，點裡面的連結接受。接受後 repo 會出現在你自己的帳號底下，網址會變成：
   ```
   https://github.com/<你的 GitHub 帳號>/wedding-invite
   ```

2. **確認 GitHub Pages 有開**：進 repo → 上方 `Settings` → 左邊選單 `Pages`。確認「Build and deployment」→「Source」是選 **GitHub Actions**（不是 "Deploy from a branch"）。轉移 repo 之後 Pages 設定有時候會被重置成沒開，如果看到是空的或選到別的選項，重新選一次「GitHub Actions」存檔。

3. **建立你自己的試算表與 Apps Script**：這一步 Chian 會親自帶你做一次，完整步驟寫在 [`apps-script/README.md`](../apps-script/README.md)。做完你會拿到一個網址（結尾通常是 `/exec`），先複製起來，下一步要用。

4. **把 Apps Script 網址存進 GitHub Secrets**：進 repo → `Settings` → 左邊選單 `Secrets and variables` → `Actions` → 按 `New repository secret`。
   - Name 填：`VITE_RSVP_ENDPOINT`
   - Secret 貼上第 3 步拿到的 Apps Script 網址
   - 按 `Add secret`

5. **手動跑一次部署**：進 repo 上方 `Actions` 分頁 → 左邊選 `Deploy to GitHub Pages` → 右邊 `Run workflow` → 選 `main` 分支 → 按綠色 `Run workflow` 按鈕。等它跑完（畫面上的圈圈變成綠色勾勾，大約 1-2 分鐘）。

6. **開網址測試**：跑完之後打開：
   ```
   https://<你的 GitHub 帳號>.github.io/wedding-invite/
   ```
   把下半的表單隨便填一筆送出（可以填「測試」當姓名），確認畫面出現「已收到您的回覆」。

7. **回試算表確認收到**：打開你在第 3 步建的試算表，「工作表1」應該多一列剛剛那筆測試資料。

8. **刪掉測試列**：確認有收到之後，回試算表把那筆測試資料整列刪掉，保持資料乾淨。

做完這 8 步，開通就完成了，之後賓客填表都會正常寫進你的試算表。

---

## 3. 換成真實內容

現在網站上的新人姓名、日期、地址等等都是**假資料**（示範用），要換成你們自己的真實資料。改法很簡單，**不用裝任何軟體**：

> **改法**：進 repo → 點進要改的檔案 → 右上角鉛筆圖示（Edit this file）→ 把假文字改成真的 → 往下拉，最下面填一句話說明改了什麼（例如「更新新人姓名」）→ 按綠色 `Commit changes` → 存檔完成，網站會在 **1～2 分鐘內自動更新**（GitHub Actions 會自動重新部署，你不用手動再做什麼）。

### 要改的地方對照表

| 要改什麼 | 在哪個檔案 | 大約第幾行 | 現在的假文字 |
| :-- | :-- | :-- | :-- |
| 網頁分頁標題 | `index.html` | 第 8 行 `<title>` | `林承宇 & 陳沛瑜 婚禮邀請函` |
| 網頁說明（搜尋結果會顯示） | `index.html` | 第 7 行 `<meta name="description">` | `林承宇 & 陳沛瑜 婚禮邀請函，誠摯邀請您出席並填寫出席意願表單。` |
| 首頁上方年份小字 | `index.html` | 第 23 行 | `DECEMBER · 2026` |
| 花環中間的日期數字 | `index.html` | 第 51 行 `<span class="hero__day">` | `19` |
| 隱藏的無障礙說明文字（螢幕報讀器會唸） | `index.html` | 第 54 行 | `2026 年 12 月 19 日，誠摯邀請您共襄盛舉` |
| 新人姓名 | `index.html` | 第 60 行 `<p class="couple__names">` | `林承宇<span class="couple__amp">&amp;</span>陳沛瑜` |
| 邀請文 | `index.html` | 第 61 行 | `誠摯邀請您與我們共度冬日的午後，<br />見證這一刻` |
| 合照 alt 說明文字 | `index.html` | 第 66 行 `alt="..."` | `林承宇與陳沛瑜的婚紗照` |
| 經文上方引言 | `index.html` | 第 71 行 | `在神的愛中　開啟新的旅程` |
| 經文內容 | `index.html` | 第 72 行 | `「所以人要離開父母，與妻子連合，二人成為一體。」` |
| 經文出處 | `index.html` | 第 73 行 | `創世記 2:24` |
| 婚宴日期 | `index.html` | 第 81 行 `<p class="details__date">` | `2026 . 12 . 19` |
| 農曆日期／星期 | `index.html` | 第 82 行 | `農曆十一月初十　·　星期六` |
| 入席時間 | `index.html` | 第 83 行 | `午宴 12:00 準時入席` |
| 教會／場地名稱 | `index.html` | 第 86 行 | `青山教會 · 主堂` |
| 地址文字 + Google 地圖連結 | `index.html` | 第 88～94 行 | 連結網址裡 `query=台北市信義區松山路一段100號`，顯示文字 `台北市信義區松山路一段 100 號` |
| 聯絡電話（含網頁上顯示與撥號連結） | `index.html` | 第 96 行 | `href="tel:0223456789"` 與顯示文字 `電話：02-2345-6789` |
| 回覆截止日 | `index.html` | 第 105 行 | `請於 2026 年 11 月 20 日前回覆，謝謝您` |
| 頁尾一句話 | `index.html` | 第 256 行 | `～ 誠摯的邀請您共享這份喜悅 ～` |
| 頁尾姓名縮寫 | `index.html` | 第 257 行 | `C &amp; P` |

> **地址／地圖連結要一起改**：第 88～94 行是同一個地方，`href="https://www.google.com/maps/search/?api=1&query=..."` 裡的 `query=` 後面接的地址，跟下面顯示給人看的地址文字，兩個都要改成新地址，兩邊要一致。可以先去 Google 地圖搜尋你們的地點，把網址列的地址複製過來。
>
> **電話同理**：`tel:0223456789` 是撥號用的純數字（不要加符號），後面 `電話：02-2345-6789` 是顯示給人看的文字，兩個都要改。

### 照片怎麼換

網站上的合照是 `public/art/couple.webp`（主要格式）和 `public/art/couple.png`（備用格式，給不支援 WebP 的舊瀏覽器）。這兩個檔案不是一般圖片，是原始照片經過壓縮腳本處理過的，**這一步對非工程師來說有點難，建議直接請 Chian 幫忙換照片**。

如果你想自己試試看，也可以：直接在 GitHub 網頁上進 `public/art/` 資料夾，把你要換的新照片改名成 `couple.webp` 跟 `couple.png`（兩個檔名要完全一樣，只是副檔名不同），上傳覆蓋原本的檔案即可（GitHub 網頁上傳檔案時，選擇「取代」就會直接蓋掉舊檔）。建議照片尺寸長邊或寬邊不要超過 1200×1800，檔案太大網站會變慢。

---

## 4. 日常怎麼看回覆

你的試算表裡有兩個工作表：

- **工作表1**：賓客每填一次表單就多一列，是最原始、最完整的資料，什麼都看得到（姓名、關係、出席狀態、人數、飲食、電話、地址、祝福留言……）。
- **統計**：這個工作表是自動算好的整理結果，分四個區塊，不用自己動手算：
  - **A:C 欄**：統計數字（出席組數、出席總人數、各種飲食需求人數、兒童椅／餐具總張數）
  - **E:G 欄**：喜餅要郵寄的名單（姓名／電話／地址）
  - **I:P 欄**：確定出席的完整名單（姓名、關係、人數、飲食、兒童椅餐具、電話、備註）
  - **R:V 欄**：禮到人不到或無法出席的名單

這個「統計」工作表的數字跟名單會**自動即時更新**，賓客一填表，這裡就會跟著變，不用手動重新整理或重跑任何東西。

---

## 5. 注意事項

- **改了 `Code.gs` 一定要重新部署才會生效**：如果以後你（或請人）去動 Apps Script 裡的程式碼（`Code.gs`），光按存檔（Ctrl+S）不會讓網址背後的程式真的更新，一定要到 Apps Script 編輯器右上角「部署」→「管理部署作業」→ 點鉛筆圖示編輯 → 版本選「新版本」→ 按「部署」，這樣改動才會生效。這是 Apps Script 最容易踩的坑，忘記做的話會覺得「我明明改了怎麼沒反應」。
- **這個 repo 是公開的（public）**：換成真實資料上線之後，新人姓名、地址、電話這些內容任何人都看得到網頁原始碼或直接打開網站看到（本來紙本喜帖上的資訊也是公開給收到的人看，性質類似，但這裡是放在網路上，任何知道網址的人都能看到，先讓你知道一下）。
- **不要動 `.github/workflows/deploy.yml`**：這個檔案是自動部署網站用的設定，不需要碰它，改了有可能讓網站部署失敗。

---

## 6. 出問題找誰

有任何問題都可以直接找 **Chian**。另外提醒一下：這整個專案都在 GitHub 上，**每一次改動都有版本紀錄**，就算不小心改壞了、存錯了,也都可以找回之前的版本還原，不用擔心把東西弄壞就回不去了。
